import generateBeamsCompound from './generate-beams-compound';
import getTabPosition from './get-tab-position';
import getKeys from './get-keys';
import addDecorations from './add-decorations';

import { getVexDuration, convertKeySignature } from './vex-utils';

const VexUtils = {
  generateBeamsCompound,
  getTabPosition,
  getKeys,
  addDecorations,
  getVexDuration,
  convertKeySignature
};

export default VexUtils;
